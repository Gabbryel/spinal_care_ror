<%= simple_form_for(specialty, id: (specialty.id ? dom_id(specialty, :edit) : dom_id(Specialty.new))) do |f| %>
<%= f.input :name %>
<%= f.input :photo %>
<%= f.rich_text_area :description, direct_upload: true %>
<div class="spec-crud">
    <button class="btn-custom btn-green btn-crud">Salvează</button>
</div>
<% end %>