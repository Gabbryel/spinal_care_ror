<!DOCTYPE html>
<html lang="ro">
  <head>
    <meta charset="UTF-8">
    <title><%= meta_title %></title>
    <link rel="canonical" href="https://spinalcare.ro">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name='description' content="<%= meta_description %>" >
    <meta name='keywords' content="<%= meta_keywords %>">
    <meta name="ahrefs-site-verification" content="42887ccc32d8cfecbea7a49857b20d58078267bb98d6a66d1da75ddb54cbe7ec">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <%= favicon_link_tag 'favicon.ico' %>
    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="<%= meta_title %>" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content="<%= meta_image %>" />
    <meta property="og:description" content="<%= meta_description %>" />
    <meta property="og:site_name" content="<%= meta_title %>" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="https://unpkg.com/trix@2.0.0/dist/trix.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  
  <body data-controller="vh sidemenu-toggle toggleAdminPanel contact" data-action="scroll@window->sidemenu-toggle#close keydown.a->toggleAdminPanel#showItWorks" tabindex="0">
    <%= render "shared/navbar" %>
    <%= render "shared/sidemenu" %>
    <%= render "shared/sidecover" %>
    <%= render "shared/gdpr" %>
    <%= render 'pages/home_partials/contact_modal' %>
    <%= render "shared/flashes" %>
    <div class="main">
      <%= yield %>
    </div>
    <%= render "shared/footer" %>
    <script type="text/javascript" src="https://unpkg.com/trix@2.0.0/dist/trix.umd.min.js"></script>
  </body>
</html>
