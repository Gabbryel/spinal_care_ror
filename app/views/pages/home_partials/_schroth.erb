<section class="schroth">
  <%#= title('Terapia Schroth', nil, nil) %>

  <%= render_feature("cabinets/www.sysphotodesign.ro_181_zv23ju.webp", "Clinica Spinal Care este una din puținele clinici de recuperare din România care are specialiști recunoscuți internațional în Terapia Schroth, una din cele mai eficiente metode de tratare a deviațiilor de coloană și torace.", true) %>
  
  <!-- <div class="schroth-title"><h3>Fiziokinetoterapeuți specializați în terapia Schroth</h3></div> -->
  <section title="specialisti schroth spinal care" class="relative w-full sm:w-[620px] md:w-[700px] lg:w-[960px] xl:w-[1150px] 2xl:w-[1400px] max-sm:gap-2 sm:gap-2 md:gap-6 lg:gap-8 xl:gap-10 2xl:gap-10  my-[7rem] lg:my-[15rem] mx-auto grid grid-cols-[repeat(auto-fit,minmax(280px,1fr))] justify-center px-3">
    <div class="relative h-full grid grid-cols-1 grid-rows-1 items-center justify-items-center max-lg:col-span-full max-lg:col-start-1 max-lg:mb-[6rem]">
      <h2 class="text-center text-4xl word-wrap font-bold px-2 py-2 font-poppins max-sm:mb-[4rem]">Specialiști în terapia Schroth</h2>
    </div>
    <% @schroths.each_with_index do |m, i| %>
          <%= render partial: "members/member_card", locals: {m: m, i: i, not_animated: nil, no_grid_area: 'no_grid_area'} %>
        <% end %>
  </section>

  <%= render_feature('cabinets/www.sysphotodesign.ro_112_knyp6k.webp', 'Specialiștii noștri sunt ajutați în munca lor de aparatură medicală performantă de ultimă generație care accelerează repararea și regenerarea organismului', false) %>
  <!-- <div class="schroth-title"><h3>Fiziokinetoterapeuți</h3></div> -->
  <article title="kinetoterapeuti spinal care bacau" class="relative <%= "#{div_widths} #{gaps_for_indexes}" %>  my-0 mx-auto grid grid-cols-[repeat(auto-fit,minmax(280px,1fr))] justify-center px-3 pt-[6rem]">
    <div class="relative h-full grid grid-cols-1 grid-rows-1 items-center justify-items-center 2xl:col-span-full 2xl:col-start-1 2xl:mb-[6rem]">
      <h2 class="text-center text-4xl word-wrap font-bold px-2 py-2 font-poppins max-sm:mb-[4rem]">Kinetoterapeuți</h2>
    </div>
    <% @kinetos_to_show.each_with_index do |m, i| %>
          <%= render partial: "members/member_card", locals: {m: m, i: i, not_animated: nil, no_grid_area: 'no_grid_area'} %>
    <% end %>
    <div class="relative h-full grid grid-cols-1 grid-rows-1 items-center justify-items-center max-sm:mt-[3rem] 2xl:col-span-full 2xl:col-start-1 2xl:mt-[3rem]">
      <h2 class="text-center text-4xl word-wrap font-bold px-2 py-2 font-poppins"><%= see_the_team(Profession.find(2).slug) %></h2>
    </div>
  </article>
  
</section>