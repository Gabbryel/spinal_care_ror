<div class="member-card <%= not_animated %> <%= no_grid_area %> <%= m.has_own_page ? 'member-card_shadow' : nil %>"
  style="transition: <%= i + 0.5 unless i.nil? %>s"
  data-controller="animation"
  data-action="scroll@window->animation#svgscroll"
  data-identity="<%= full_name(m) %>" data-profession="<%= m.profession.name %>" data-specialty="<%= m.specialty.name if m.specialty %>">
  <% if m.has_own_page %>
    <a href="../echipa/<%= m.slug %>" id="<%= m.slug %><%= path_for_id %>" name="<%= m.slug %><%= path_for_id %>" >
      <%= render 'members/member_card_info', m: m %>
    </a>
  <% else %>
    <%= render 'members/member_card_info', m: m %>
  <% end %>
</div>
